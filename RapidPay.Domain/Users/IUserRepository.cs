namespace RapidPay.Domain.User
{
    public interface IUserRepository
    {
        //Task<User?> GetByUsernameAndPassword(string userName, string password, CancellationToken cancelationToken);

        //Task<bool> ExistsUsername(string username, CancellationToken cancelationToken);

        //Task Insert(User user, CancellationToken cancelationToken);
    }
}
